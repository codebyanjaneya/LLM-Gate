[pytest]
# Register custom markers so `--strict-markers` stays clean.
markers =
    security: security-focused checks (may fail against the intentionally insecure demo app)

testpaths = tests
addopts = -v --strict-markers
